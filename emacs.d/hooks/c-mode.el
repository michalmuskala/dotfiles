;; line numbers
(linum-mode)

;; show junk whitespace
(whitespace-mode)

;; activate character pairing
(smartparens-mode)

(setq c-default-style "k&r"
      c-basic-offset 4)

;; autocomplete
(company-mode)
