;; Don't compile at save - usually framework does that for us
(setq scss-compile-at-save nil)

;; show line numbers
(linum-mode)

;; show junk whitespace
(whitespace-mode)

;; activate character pairing
(smartparens-mode)
